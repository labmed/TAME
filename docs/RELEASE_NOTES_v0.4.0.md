# tametools 0.4.0

Downloads include a Windows installer, a portable Windows app, a local browser bundle, and Python packages.

The browser bundle requires Python and internet access for initial setup. Windows installer and portable packages include their Python runtime.

See `SHA256SUMS.txt` for file checksums and `BUILD_VERIFICATION.json` for build verification.
